// 🚫 Firebase completely disabled

console.log("Firebase disabled successfully");

const auth = null;
const googleProvider = null;
const githubProvider = null;

export { auth, googleProvider, githubProvider };